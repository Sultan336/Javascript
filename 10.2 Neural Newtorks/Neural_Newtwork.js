Perceptron P;

void setup() {
    size(200, 200);
    P = new Perceptron();
    float inputs = {
        -1,
        0.5
    }
    int guess = p.guess(inputs);
    console.log(guess)
}

void draw() {

}